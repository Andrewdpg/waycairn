node_modules
dist

# build/typecheck byproducts
*.tsbuildinfo
ui/vite.config.js
ui/vite.config.d.ts

# logs
npm-debug.log*

# editor/OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# env
.env
.env.*
!.env.example

# local docs/specs/plans, not committed
docs/superpowers

# subagent-driven-development scratch (briefs, reports, diffs, ledger)
.superpowers/

# waycairn's derived sqlite index — a binary git can't merge; the .json
# artifact files under .waycairn/<kind>/ are the versioned source of truth
.waycairn/index.sqlite
